import { BtnPDirective } from './btn-p.directive';

describe('BtnPDirective', () => {
  it('should create an instance', () => {
    const directive = new BtnPDirective();
    expect(directive).toBeTruthy();
  });
});
